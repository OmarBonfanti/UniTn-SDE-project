### CLOUDFLARE WORKER (Optional/Extra)

The project includes a Cloudflare Worker to manage [write here what it does, e.g., the maps/proxy APIs].
The source code is located in the /worker folder.

Main files:

- src/index.js: Worker logic.
- wrangler.toml: Configuration (account_id removed for privacy).
